<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.avi.in.spex_realmex7pro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar">
        <activity android:name=".specScroll"></activity>
        <activity
            android:name=".fullSpec"
            android:label="@string/title_activity_full_spec"
            android:theme="@style/NoActionBar" />
        <activity android:name=".battery" />
        <activity android:name=".camera" />
        <activity android:name=".Display" />
        <activity android:name=".Processor" />
        <activity android:name=".MainActivity" />
        <activity android:name=".WelcomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>